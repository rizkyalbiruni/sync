<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SYNC</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <header class="app-header">
        <h1>SYNC</h1>
    </header>

    <main class="app-content">
        <section class="introduction">
            <p>Where minds connect...</p>
        </section>

        <div data-controller="instructions">
            <button data-action="click->instructions#reveal" class="btn btn-outline-primary">How it works</button>
            <div data-instructions-target="revealInstructions" class="d-none">
                <%= render "instructions" %>
            </div>
        </div>

        <div data-controller="toggle" class="mt-5">
            <button class="btn btn-outline-primary" data-action="click->toggle#fire">Create a Group</button>
            <div data-toggle-target="createGroup" class="d-none">
                <%= simple_form_for(@group) do |f| %>
                <%= f.input :name %>
                <%= f.submit %>
                <% end %>
            </div>
        </div>

        <% if current_user %>
        <div class="justify-content-center d-flex">
            <ul>
                <% @groups.each do |group| %>
                <li><%= group.name %></li>
                <% group.users.each do |user|%>
                <p>players :<%= user.name %></p>
                <% end %>
                <%= link_to "Start Game", group_games_path(group), data: {turbo_method: :post} %>
                <% end %>
            </ul>
        </div>
        <% end %>
    </main>
</body>
</html>
